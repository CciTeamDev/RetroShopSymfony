{% extends 'base.html.twig' %}

{% block title %}Comments{% endblock %}

{% block body %}
    <h1>Comments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ comment.createdAt ? comment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ comment.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comments_index') }}">Revenir à tous les commentaires</a>

    {# <a href="{{ path('comments_edit', {'id': comment.id}) }}">edit</a> #}

   {{ include('comments/_delete_form.html.twig') }}
{% endblock %}
